# SGSP 同步系统优化计划与源码审计

> 项目：`MbAIGC/SGSP`  
> 参考项目：思源笔记（SiYuan）  
> 目标：完善 SGSP 的同步错误处理、通知系统、GitHub 同步可靠性，并优化 GitHub 端的数据组织与提交历史。

---

# 一、总体优化目标

SGSP 当前已经解决了一部分“**冲突发生之后怎么办**”的问题，但仍存在三个核心短板：

1. **同步错误没有形成统一的错误体系，很多底层错误无法可靠到达前端。**
2. **GitHub 同步依赖原始同步算法和 `lastSyncCommit`，多设备、远端变化、Push 被拒绝等场景比较脆弱。**
3. **GitHub 仓库和 Commit 历史容易出现混乱，缺乏面向长期同步的仓库组织策略。**

因此建议把 SGSP 后续改造目标定义为：

> **从“能同步、能处理冲突”升级到“可观测、可恢复、高成功率、GitHub 数据清晰”的 Sync Engine 2.0。**

---

# 二、参考思源笔记的设计思路

思源笔记本身具有比较成熟的插件体系和事件机制，可以重点参考：

- 插件生命周期
- Plugin API
- Event Bus / 事件总线
- 插件事件监听
- 插件存储
- 前端通知与状态反馈

SGSP 不需要照搬思源插件体系，而应该借鉴其核心思想：

```text
同步核心
   ↓
事件
   ↓
状态
   ↓
前端 UI
```

而不是：

```text
同步核心
   ↓
console.error()
   ↓
用户不知道发生了什么
```

---

# 三、当前 SGSP 的总体架构

当前 SGSP 本质上是：

```text
原版 GIT-SYNC-PLUGIN v0.3.0
            │
            ▼
     vendor/index.js
            │
            │ patch
            ▼
┌─────────────────────────┐
│ src/sync-flow-runtime.js │
└────────────┬────────────┘
             │
             ▼
    包装 syncDataToCloud()
             │
             ▼
    原版 Git 同步算法
```

项目当前采用的是“**原始 bundle + patch 注入 + runtime 扩展**”模式。

因此不能简单粗暴地把整个 `vendor/index.beautified.js` 推倒重写。

应该优先：

- 保留现有冲突状态机
- 保留现有补丁机制
- 在关键 Git 操作位置增加可靠性层
- 逐步将同步核心从“原始实现”抽象出来

---

# 四、当前状态：已经做得比较好的部分

## 4.1 冲突处理闭环

当前 SGSP 的冲突处理已经比较完整：

```text
冲突
 ↓
CONFLICT
 ↓
暂停自动同步
 ↓
CONFLICT_PAUSED
 ↓
保存状态
 ↓
显示红色状态
 ↓
冲突弹窗
 ↓
用户选择：
  ├─ 保留本地
  ├─ 保留远端
  └─ 稍后处理
 ↓
RESOLVING
 ↓
成功
 ↓
RESOLVED
 ↓
恢复自动同步
```

这一套逻辑建议保留，不要推翻。

---

## 4.2 已经针对错误吞掉问题做过 patch

原始代码部分：

```text
handleRemoteCoverLocal
handleLocalCoverRemote
handleAutoRemoteAndLocalFileSync
```

存在：

```text
rethrow: false
```

这会导致：

```text
底层错误
 ↓
错误处理器
 ↓
异常被吞掉
```

SGSP 已经将这些关键位置修改为：

```text
rethrow: true
```

这是正确方向。

但是：

> **重新抛出错误 ≠ 已经建立完整错误系统。**

后续仍需要统一错误分类、错误持久化、错误通知和恢复机制。

---

# 五、源码审计：src/

当前 `src/` 的核心是：

```text
src/sync-flow-runtime.js
```

它已经承担：

- 同步状态
- 冲突状态
- 冲突恢复
- 通知
- 日志
- wrapper

但目前最大问题是：

> **它实际上更像“冲突状态机”，而不是完整的同步状态机。**

---

# 六、当前状态机存在的问题

目前大致有：

```text
idle
running
success
failed
conflict
conflict_paused
resolving
resolved
```

问题在于：

```text
failed
```

太笼统。

无法区分：

```text
网络错误
Token 错误
权限错误
Repository 不存在
Push 被拒绝
远端发生变化
大文件
本地文件错误
Git 错误
未知错误
```

---

# 七、建议升级为完整 Sync State

建议状态：

```text
IDLE

CHECKING
FETCHING
MERGING
COMMITTING
PUSHING

SUCCESS

CONFLICT
PAUSED

ERROR_NETWORK
ERROR_AUTH
ERROR_REPOSITORY
ERROR_PERMISSION
ERROR_FILE
ERROR_GIT
ERROR_REMOTE_CHANGED
ERROR_LARGE_FILE
ERROR_UNKNOWN

RETRYING
RESOLVING
```

例如：

```js
{
  state: "ERROR_NETWORK",
  phase: "push",
  message: "无法连接 GitHub",
  detail: "...",
  retryable: true,
  timestamp: 1756789123,
  operationId: "sync-xxxx"
}
```

---

# 八、当前“前端很多错误不显示”的根本原因

当前 `runSync()` 已经尝试：

```text
原同步函数
 ↓
catch
 ↓
判断 conflict
 ↓
FAILED
 ↓
notify()
 ↓
throw err
```

因此问题并不是完全没有通知，而是：

> **错误传播链不完整、错误包装层级复杂、错误类型没有标准化。**

原始同步代码存在大量错误包装：

```text
Fe
Ht
Jt
Mr
...
```

SGSP 当前主要通过 `cause` 链寻找底层错误。

但这种方案不够稳定。

---

# 九、错误摘要提取存在问题

当前错误提取逻辑可能过早停止。

例如：

```text
外层错误
 ├─ status = 500
 ├─ message = "同步失败"
 │
 └─ cause
      └─ 文件上传错误
           └─ xxx.sy
```

如果一看到外层 `status` 就停止，最终用户只能看到：

```text
HTTP 500：同步失败
```

而看不到真正有价值的信息：

```text
xxx.sy 上传失败
```

---

# 十、建议增加统一 SyncError

增加：

```text
normalizeGitError()
```

统一转换底层异常：

```js
{
  category: "NETWORK",
  code: "TIMEOUT",
  httpStatus: 0,
  operation: "push",
  path: "...",
  retryable: true,
  recoverable: true,
  message: "...",
  detail: "...",
  action: "retry"
}
```

---

# 十一、建议建立错误分类

| 类型 | 用户看到 | 自动重试 |
|---|---|---:|
| NETWORK | GitHub 网络连接失败 | ✅ |
| TIMEOUT | GitHub 请求超时 | ✅ |
| DNS | 无法解析 GitHub | ✅ |
| AUTH | Token 无效 | ❌ |
| PERMISSION | Token 权限不足 | ❌ |
| NOT_FOUND | 仓库不存在 | ❌ |
| BRANCH | 分支不存在 | ❌ |
| REMOTE_CHANGED | 远端发生变化 | ✅ |
| PUSH_REJECTED | Push 被拒绝 | ✅ |
| CONFLICT | 文件冲突 | ❌ |
| LARGE_FILE | 文件过大 | ❌ |
| LOCAL_FILE | 本地文件异常 | ❌ |
| GIT | Git 状态异常 | 视情况 |
| UNKNOWN | 未知错误 | 有限重试 |

---

# 十二、日志系统问题

当前日志主要保存在：

```text
host.logEntries
```

并且存在数量限制。

问题：

```text
发生错误
 ↓
日志存在
 ↓
思源重启
 ↓
日志消失
```

这对于自动同步尤其不利。

建议增加持久化同步历史：

```text
.sgsp/
└── sync-state.json
```

例如：

```json
{
  "lastSync": "...",
  "lastSuccess": "...",
  "lastError": {
    "category": "NETWORK",
    "message": "...",
    "timestamp": "..."
  },
  "history": []
}
```

至少保存最近 N 次同步结果。

---

# 十三、通知系统改造

建议不要继续依赖零散 Toast / Modal。

应该建立：

```text
Sync Event Bus
       │
       ├── sync:start
       ├── sync:progress
       ├── sync:success
       ├── sync:error
       ├── sync:conflict
       ├── sync:paused
       ├── sync:resumed
       └── sync:retry
```

前端：

```js
syncEvents.on("sync:error", showSyncError)

syncEvents.on("sync:conflict", showConflictDialog)

syncEvents.on("sync:success", showSyncSuccess)
```

---

# 十四、建议增加 Notification Center

顶栏显示：

```text
🟢 已同步
🔄 同步中
🟡 等待处理
🔴 同步失败
⚠️ 存在冲突
```

点击后：

```text
┌─────────────────────────────┐
│ Git 同步                     │
├─────────────────────────────┤
│ 🔴 同步失败                  │
│ GitHub 网络连接失败          │
│ 2 分钟前                     │
│                             │
│ [重试] [查看详情]             │
├─────────────────────────────┤
│ ⚠️ 发现 3 个冲突              │
│ 10 分钟前                    │
│                             │
│ [处理冲突]                    │
├─────────────────────────────┤
│ 🟢 上次同步成功               │
│ 2026-09-02 16:32             │
└─────────────────────────────┘
```

通知应该分：

- 普通
- 警告
- 错误

错误必须支持：

- 查看详情
- 重试
- 处理
- 忽略

---

# 十五、增加 SyncContext

每一次同步都应该产生一个上下文：

```js
{
  id,
  trigger,
  startedAt,

  baseCommit,
  localCommit,
  remoteCommit,

  localChanged,
  remoteChanged,

  phase,

  result,
  error
}
```

例如：

```text
SYNC-20260902-001

触发方式：自动

BASE：abc123
LOCAL：def456
REMOTE：789abc

本地变化：12
远端变化：4

阶段：
✓ fetch
✓ compare
✓ merge
✗ push

错误：
HTTP 409
remote HEAD changed

建议：
重新同步
```

这样通知系统、日志、诊断和同步状态都可以共享同一份数据。

---

# 十六、源码审计：patch/

当前：

```text
patch/apply-patch.mjs
```

采用字符串 Anchor 注入。

大致：

```text
寻找固定字符串
 ↓
assertAnchor()
 ↓
replace()
 ↓
注入 runtime
```

例如：

```text
原版 syncDataToCloud
        ↓
__gSyncDataToCloudBase()
        +
新的 syncDataToCloud()
```

---

# 十七、patch 机制的主要风险

最大问题：

> **原始 bundle 升级以后，字符串 patch 很容易失效。**

例如：

```text
v0.3.0
 ↓
v0.3.1
```

原代码变化：

```text
async syncDataToCloud(t=void 0,s=!1)
```

变成：

```text
async syncDataToCloud(t,s)
```

可能导致：

```text
assertAnchor
 ↓
找不到
 ↓
构建失败
```

这是可以接受的。

更危险的是：

```text
Anchor 仍然存在
 ↓
patch 成功
 ↓
原始语义已经变化
 ↓
patch 逻辑悄悄失效
```

---

# 十八、建议给 patch 增加 Bundle Fingerprint

例如：

```json
{
  "sourceVersion": "0.3.0",
  "sourceSha256": "...",
  "anchors": {
    "syncDataToCloud": "...",
    "handleAutoSync": "..."
  }
}
```

构建时同时检查：

```text
vendor version
vendor SHA256
anchor
```

防止“patch 成功但语义已经不兼容”。

---

# 十九、源码审计：vendor/index.beautified.js

这里是整个项目最值得关注的部分。

关键方法包括：

```text
handleAutoRemoteAndLocalFileSync()
handleRemoteCoverLocal()
handleLocalCoverRemote()
compareCommitFiles()
commitAndPushFileToRemote()
addFileToWorkArea()
```

这里才真正决定：

> SGSP 同步是否可靠。

---

# 二十、当前同步算法的核心

大致：

```text
lastSyncCommit
      ↓
getCommitInfo(lastSyncCommit)
      ↓
remote HEAD
      ↓
compareCommitFiles(lastSyncCommit, remote HEAD)
      ↓
得到远端修改
      ↓
读取本地修改
      ↓
处理合并
      ↓
commit
      ↓
push
```

---

# 二十一、同步条件苛刻的第一个核心原因

当前高度依赖：

```text
lastSyncCommit
```

例如：

```text
设备 lastSync = B

GitHub 后来：
X → Y → Z
```

如果 B 已经不在远端历史里：

```text
getCommitInfo(B)
```

可能直接失败。

当前体系没有完整的：

```text
BASE 不存在
 ↓
寻找共同祖先
 ↓
重新建立同步基准
 ↓
三方合并
```

因此：

> `lastSyncCommit` 失效是当前同步可靠性的一个重要薄弱点。

---

# 二十二、当前 compareCommits 并不等于完整三方同步

当前大量依赖 GitHub：

```text
compareCommits(base, head)
```

它本质是：

> 两个 commit 的差异比较。

并不是完整的：

> Git merge-base + three-way merge。

所以当前实现更准确地说是：

```text
基于 lastSync SHA 的远端变化检测
+
本地工作区变化合并
```

而不是完整 Git 三方同步引擎。

---

# 二十三、Push 的第二个核心问题：缺少完整 CAS 保护

当前流程大致：

```text
createTree
 ↓
createCommit
 ↓
updateRef
```

缺少一个完整的：

```text
push 前确认 remote HEAD
=
刚刚读取的 HEAD
```

场景：

```text
设备 A              GitHub              设备 B

读取 HEAD=A
                     HEAD=A
                                          读取 HEAD=A

创建 B
                     HEAD=A
                                          创建 C

push B
                     HEAD=B

                                          push C
```

理想情况：

```text
C push 被拒绝
 ↓
fetch
 ↓
merge
 ↓
重新生成 commit
 ↓
push
```

这套自动恢复目前并不完整。

---

# 二十四、建议增加 Push CAS / Remote HEAD 验证

同步过程中记录：

```text
expectedRemoteHead
```

Push 前再次确认：

```text
currentRemoteHead
```

如果：

```text
currentRemoteHead !== expectedRemoteHead
```

不要直接覆盖。

进入：

```text
REMOTE_CHANGED
```

然后：

```text
fetch
 ↓
重新比较
 ↓
merge
 ↓
retry push
```

---

# 二十五、当前 GitHub Commit 混乱的核心原因

`commitAndPushFileToRemote()` 会根据工作区内容进行分批。

当超过阈值：

```text
commit
 ↓
slice++
 ↓
继续
 ↓
再次 commit
```

原始 Commit Message 还可能直接使用：

```text
时间 + slice
```

结果 GitHub 容易变成：

```text
sync 1
sync 2
sync 3
sync 4
sync 5
...
```

---

# 二十六、建议：一次同步尽量只产生一个 Commit

理想：

```text
一次同步

├── 12 个 sy
├── 18 张图片
├── 1 个字体
└── 3 个删除

        ↓

一个 Tree
        ↓
一个 Commit
        ↓
一个 Push
```

GitHub：

```text
sync: update 34 files
```

只有超过 GitHub/API 或大文件限制时才真正分批。

---

# 二十七、Commit Message 规范

建议：

```text
sync: initial repository

sync: update notes

sync: update notebook 技术

sync: update 18 files

sync: update 12 notes + 6 assets

sync: pull remote changes

sync: resolve conflict

sync: restore files
```

避免：

```text
1725261223123_1
1725261223123_2
1725261223123_3
```

---

# 二十八、同步前必须进行无变化检测

同步流程增加：

```text
git status / 工作区检测
 ↓
过滤 ignored
 ↓
过滤临时文件
 ↓
过滤同步状态文件
 ↓
计算 diff
 ↓
没有变化
 ↓
直接 SUCCESS
```

不要：

```text
无变化
 ↓
commit
 ↓
push
```

这样可以大量减少 GitHub 无意义提交。

---

# 二十九、大文件问题

当前 patch 已经增加 Blob 请求大小预检。

它能够：

```text
检测文件大小
 ↓
估算 Base64 / Request size
 ↓
提前拒绝过大的请求
```

这是正确方向。

但它解决的是：

> “不要让 GitHub API 因为请求太大而直接失败。”

并没有解决：

> “大文件应该怎么同步。”

---

# 三十、大文件建议

可以根据文件大小：

```text
< 10 MB
    ↓
普通 Git

10~50 MB
    ↓
警告

> 50 MB
    ↓
建议 LFS / 外部存储

> 100 MB
    ↓
普通 Git 禁止
```

前端应该显示：

```text
⚠️ 检测到大文件

fonts/xxx.ttf
大小：68 MB

GitHub 普通 Git 不适合直接保存此文件。

[查看文件]
[使用大文件方案]
```

而不是最后只显示：

```text
同步失败
```

---

# 三十一、测试审计

当前：

```text
tests/sync-flow.test.mjs
```

测试覆盖主要集中在：

- 初始状态
- 正常同步
- 冲突
- 自动暂停
- 手动处理
- 本地覆盖
- 远端覆盖
- 解决失败
- 普通错误
- HTTP 413
- 重启恢复
- 稍后处理
- 手动模式
- wrapper

### 优点

状态机测试覆盖已经不错。

### 不足

真正的 Git 同步测试明显不足。

目前更多是：

```text
Fake Plugin
 ↓
Fake Sync Function
 ↓
Fake Error
 ↓
State Machine
```

缺少：

```text
真实 GitHub
 ↓
真实 Fetch
 ↓
真实 Compare
 ↓
真实 Tree
 ↓
真实 Commit
 ↓
真实 Push
```

---

# 三十二、测试中的异步问题

部分测试使用：

```js
.catch(...)
```

但没有充分使用：

```js
await
```

或者：

```js
assert.rejects()
```

建议改成：

```js
await assert.rejects(...)
```

确保测试框架真正等待异步异常。

---

# 三十三、CONFLICT-WORKFLOW.md 审计

当前冲突文档整体设计是合理的。

它清楚描述：

```text
冲突
 ↓
暂停
 ↓
通知
 ↓
用户处理
 ↓
恢复
```

真正暴露出来的问题是：

> 当前 SGSP 重点解决的是“冲突之后怎么办”，并没有真正重写“冲突发生前的同步算法”。

因此：

```text
CONFLICT-WORKFLOW
```

目前更像：

> 冲突恢复系统

而不是：

> 完整 Sync Engine。

---

# 三十四、建议的核心架构

最终建议：

```text
                    SGSP
                     │
                     ▼
             ┌───────────────┐
             │ Sync Controller│
             └───────┬───────┘
                     │
        ┌────────────┼────────────┐
        ▼            ▼            ▼
     State         Error         Git
     Machine       Engine        Engine
        │            │            │
        │            │            ├── Fetch
        │            │            ├── Compare
        │            │            ├── Merge
        │            │            ├── Commit
        │            │            └── Push
        │            │
        │            └── Normalize
        │                Classify
        │                Retry
        │
        ▼
 Notification Center
        │
        ├── Toast
        ├── Badge
        ├── Dialog
        └── History
```

---

# 三十五、推荐的 Git 同步流程

```text
获取本地状态
      ↓
获取远端 HEAD
      ↓
获取 BASE Commit
      ↓
判断三方关系
      │
 ┌────┼─────────────┐
 │    │             │
 ▼    ▼             ▼
同源  远端领先      本地领先
 │    │             │
 ▼    ▼             ▼
同步  Fetch         Push
      /Merge
        │
        ▼
   双方都有变化
        │
        ▼
   Three Way Merge
        │
    ┌───┴───┐
    ▼       ▼
 无冲突    有冲突
    │       │
    ▼       ▼
  Commit   Conflict
    │
    ▼
Push 前重新检查 HEAD
    │
    ├── 未变化 → Push
    │
    └── 已变化
          ↓
       Fetch
          ↓
       Merge
          ↓
       Retry
```

---

# 三十六、建议建立 BASE Commit

每次成功同步保存：

```text
lastSyncedCommit
```

含义：

> 本地与远端最后一次被双方认可的共同状态。

之后：

```text
BASE
 │
 ├── LOCAL
 │
 └── REMOTE
```

进行三方判断。

这样才能正确处理：

```text
A 手机修改
B 手机修改
GitHub 又发生变化
```

---

# 三十七、Sync Engine 2.0 建议路线

## SGSP 0.4 —— 稳定性

目标：

> 所有同步错误都能可靠到达前端。

任务：

- [ ] 统一 SyncState
- [ ] 统一 SyncError
- [ ] Git 错误分类
- [ ] 网络错误分类
- [ ] Token 错误
- [ ] Permission 错误
- [ ] Repository 错误
- [ ] 冲突错误
- [ ] 所有异常进入通知系统
- [ ] 自动重试
- [ ] 指数退避
- [ ] 防止重复同步

---

# 三十八、SGSP 0.5 —— 通知系统

目标：

> 用户任何时候都知道同步发生了什么。

任务：

- [ ] 顶栏同步状态
- [ ] 红 / 黄 / 绿状态
- [ ] Notification Center
- [ ] 同步进度
- [ ] 错误详情
- [ ] 一键重试
- [ ] 一键解决冲突
- [ ] 一键打开相关文件
- [ ] 通知历史
- [ ] 通知去重
- [ ] 通知持久化
- [ ] 重启后恢复状态

---

# 三十九、SGSP 0.6 —— 同步引擎

目标：

> 降低“同步条件苛刻”导致的失败。

任务：

- [ ] BASE Commit
- [ ] 三方同步
- [ ] 自动 Fetch
- [ ] 自动判断远端领先
- [ ] 自动 Merge
- [ ] Push rejected 自动恢复
- [ ] 网络失败自动 Retry
- [ ] Token 错误不 Retry
- [ ] 冲突进入 PAUSED
- [ ] 解决冲突后自动恢复
- [ ] Remote HEAD CAS 检查
- [ ] lastSync SHA 失效恢复

---

# 四十、SGSP 0.7 —— GitHub 仓库优化

目标：

> GitHub 仓库干净、清晰、可读。

任务：

- [ ] 仓库目录规划
- [ ] notes/
- [ ] assets/
- [ ] system/
- [ ] `.sgsp/`
- [ ] 自动生成 README
- [ ] Commit Message 规范
- [ ] 减少无意义 Commit
- [ ] 文件变化检测
- [ ] Assets 分类
- [ ] 大文件检测
- [ ] Git LFS 支持

---

# 四十一、SGSP 0.8 —— 诊断系统

目标：

> 从“能同步”升级为“可以自己解释为什么不能同步”。

增加：

```text
同步诊断
```

显示：

```text
Git 状态             ✅ 正常
GitHub API           ✅ 正常
Token                ✅ 有效
Repository           ✅ 正常
Branch               ✅ main
本地 Git             ✅ 正常

远端 HEAD            a83f12c
本地 BASE             a83f12c

本地变化             3 个文件
远端变化             0 个文件
未提交文件           3
冲突文件             0
```

诊断项目：

```text
✓ Git 是否安装
✓ Git 版本
✓ workspace 是否存在
✓ data 是否存在
✓ Git repository
✓ remote URL
✓ Token
✓ GitHub API
✓ Branch
✓ Fetch
✓ Push 权限
✓ 文件权限
✓ 大文件
✓ ignored 文件
```

最终：

```text
🟢 12 项正常
🟡 1 项警告
🔴 0 项错误
```

---

# 四十二、GitHub 数据组织建议

这里建议分两个阶段。

## 第一阶段

先不要急着大改目录。

先解决：

```text
同步正确
同步可靠
Commit 正确
```

否则同步算法和目录结构同时改变，会很难排查问题。

## 第二阶段

再考虑：

```text
SGSP/
├── README.md
├── .gitignore
├── .sgsp/
│   ├── config.json
│   ├── sync-state.json
│   └── version
│
├── notes/
│   ├── Notebook A/
│   └── Notebook B/
│
├── assets/
│   ├── image/
│   ├── attachment/
│   └── ...
│
└── system/
    ├── templates/
    └── snippets/
```

---

# 四十三、进一步建议：Git 存储与 GitHub 展示分离

长期可以考虑：

```text
                 SiYuan
                    │
                    ▼
             SGSP Sync Layer
                    │
          ┌─────────┴─────────┐
          │                   │
          ▼                   ▼
     Git Storage        Display Storage
          │                   │
       .sy files            Markdown
       assets
```

Git 内部可以保存思源所需的数据。

GitHub 展示层则可以更加人性化。

例如自动生成：

```text
README.md

# 我的思源笔记

## 笔记本

- 技术
- 工作
- 随笔
- 项目

最后同步：
2026-09-02 16:32
```

这样 GitHub 不再只是一个“思源数据转储目录”。

---

# 四十四、优先级重新排序

## P0 —— 必须优先解决

### 1. 所有错误不能丢失

```text
底层错误
 ↓
统一捕获
 ↓
统一 Error
 ↓
前端必达
```

### 2. Push 前 Remote HEAD 检查

### 3. Remote HEAD 变化后的自动恢复

```text
409
 ↓
Fetch
 ↓
Merge
 ↓
Retry
```

### 4. lastSyncCommit 失效恢复

### 5. 同步锁 / SyncQueue

避免：

```text
自动同步
+
手动同步
+
冲突解决
```

同时执行。

---

# 四十五、P1

- [ ] 错误分类
- [ ] SyncContext
- [ ] 持久化同步记录
- [ ] Notification Center
- [ ] 自动 Retry
- [ ] 指数退避
- [ ] Remote Changed 状态
- [ ] 大文件友好提示

---

# 四十六、P2

- [ ] Commit 优化
- [ ] 一次同步尽量一个 Commit
- [ ] Commit Message 规范
- [ ] 减少无意义提交
- [ ] 文件变化检测
- [ ] GitHub 仓库 README

---

# 四十七、P3

- [ ] GitHub Repository Layout
- [ ] notes/
- [ ] assets/
- [ ] system/
- [ ] GitHub 展示优化
- [ ] 大文件/LFS
- [ ] Markdown 展示层

---

# 四十八、最终源码改造重点

最值得保留：

```text
src/sync-flow-runtime.js
```

尤其：

```text
CONFLICT_PAUSED
冲突持久化
红色 Badge
冲突 Dialog
恢复自动同步
```

重点需要改造：

```text
vendor/index.beautified.js
```

尤其：

```text
handleAutoRemoteAndLocalFileSync()
compareCommitFiles()
commitAndPushFileToRemote()
addFileToWorkArea()
Git API 错误处理
```

同时调整：

```text
patch/apply-patch.mjs
```

增加：

```text
Bundle Fingerprint
更可靠 Anchor
同步核心扩展点
错误传播扩展点
Push/HEAD 检查扩展点
```

---

# 四十九、最终判断

当前 SGSP 可以概括为：

> **已经把“冲突发生后的用户体验”修好了，但“冲突发生前的同步可靠性”和“所有异常的可观测性”还没有真正解决。**

因此下一阶段不应该继续单纯增加：

```text
弹窗
Toast
Badge
```

而应该优先建立：

```text
Sync Engine 2.0
      │
      ├── Sync State
      ├── Sync Context
      ├── Sync Error
      ├── Error Classifier
      ├── Retry Engine
      ├── BASE Commit
      ├── Remote HEAD CAS
      ├── Three-way Merge
      └── Notification Event Bus
```

最终形成：

```text
                 SGSP
                  │
                  ▼
          Sync Controller
                  │
      ┌───────────┼───────────┐
      ▼           ▼           ▼
    State       Error        Git
    Machine     Engine       Engine
      │           │           │
      │           │           ├─ Fetch
      │           │           ├─ Compare
      │           │           ├─ Merge
      │           │           ├─ Commit
      │           │           └─ Push
      │           │
      │           └─ Normalize
      │              Classify
      │              Retry
      │
      ▼
 Notification Center
      │
      ├─ Toast
      ├─ Badge
      ├─ Dialog
      └─ History
```

---

# 五十、推荐下一步

不要立即让 Codex / DSH 对整个 `vendor/index.beautified.js` 进行大规模修改。

建议先做一份：

## 《SGSP Sync Engine 2.0 详细改造设计》

具体到代码级别：

```text
1. src/sync-flow-runtime.js
   ├─ 哪些函数保留
   ├─ 哪些函数修改
   └─ 哪些函数新增

2. vendor/index.beautified.js
   ├─ 哪些函数必须 patch
   ├─ 哪些逻辑应该抽出来
   └─ 哪些原逻辑不能动

3. patch/apply-patch.mjs
   ├─ 新增哪些 Anchor
   ├─ 如何做版本检测
   └─ 如何避免 bundle 升级导致静默失效

4. SyncError
   └─ 完整错误结构

5. SyncContext
   └─ 完整数据结构

6. SyncState
   └─ 完整状态转换图

7. GitHub Push
   ├─ HEAD 检查
   ├─ 409 恢复
   └─ Retry

8. BASE Commit
   └─ lastSync SHA 失效恢复

9. Notification Center
   └─ UI 与 Event Bus 接口

10. 测试
    ├─ 状态机
    ├─ 错误分类
    ├─ 并发 Push
    ├─ Remote Changed
    ├─ BASE 丢失
    └─ GitHub 集成测试

11. Codex / DSH 实施任务
    └─ 按阶段拆成可执行任务
```

**最重要的是：先设计接口和状态机，再修改 vendor。**  
否则直接让 AI 改压缩/美化后的原始 bundle，很容易把目前已经比较成熟的冲突处理逻辑破坏掉。
