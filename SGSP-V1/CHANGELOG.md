# 更新日志 / Changelog

> 本文件由仓库维护,不再依赖外部文档链接。
> 历史版本条目来源于 GitHub Releases;`0.3.0-dev-00` 为本次重构开发版
> (构建产物即仓库根目录插件包,CI 自动打包,见 `.github/workflows/build.yml`)。

## SGSP（重命名版）

- 为避免与原插件 `sy-git-sync-plugin` 重名，Fork 后将插件标识、显示名称、安装目录和 CI 产物统一改为 `SGSP`。
- 当前维护者：`MbAIGC`。
- 感谢并致敬原作者 [xstarling](https://github.com/xstarling)；原项目：[xstarling/sy-git-sync-plugin](https://github.com/xstarling/sy-git-sync-plugin)。

## 0.3.0-dev-00 (重构开发版 / Refactor dev build)

本次重构 fork 自 v0.3.0 发布包,解决两大问题:

1. **同步状态通知问题**
   - 新增同步状态机(`idle / running / success / failed / conflict / conflict_paused / resolving / resolved`)。
   - 冲突发生时顶栏图标变为 🔴 红色闪烁徽标,并弹出**持久化冲突处理对话框**。
   - 自动同步不再在冲突时反复弹出瞬时错误提示(一个暂停会话只提示一次)。

2. **冲突处理闭环**(发现冲突 → 🔴 冲突状态 → 暂停自动同步 → 通知用户 → 用户选择 → 解决冲突 → 🟢 恢复自动同步)
   - 冲突发生时**自动暂停自动同步定时器**,避免反复尝试提交。
   - 用户可选择:保留本地版本 / 保留远端版本 / 打开冲突文档 / 稍后处理。
   - 选择保留本地/保留远端后自动执行强制覆盖同步,成功后**自动恢复自动同步**。
   - 冲突暂停状态会持久化,**重启思源后仍保持暂停**,直到用户处理完毕。

3. **文档修复**
   - README(中/英)重写:修正术语错误(如 "Source Han Notes" → 思源笔记/SiYuan notes)、错别字、自相矛盾的说明。
   - 更新日志、功能列表改为仓库内自包含,外部金山文档链接降级为补充阅读。
   - 新增:同步状态与冲突处理章节、项目结构说明。

实现方式:对官方 v0.3.0 打包产物做源码级补丁注入(不修改任何同步算法),
见 `docs/REFACTOR-NOTES.md` 与 `patch/apply-patch.mjs`。

## v0.3.0 (官方 / Official)

- 新增功能:用户自定义同步到 git 仓库的 `assets/` 资源文件的路径
- 新增功能:用户下拉菜单,选择更新本地资源文件(恢复因为配置 `assets/` 资源文件路径导致本地资源不能正常显示)、刷新工作树(恢复因为数据同步后,部分新增笔记的 ID 不能找到的问题)
- 修复 BUG:修复上次更新后,部分用户反馈同步数据提示【请初始化远端仓库 / 检查设置界面的 token 配置是否正确 / 检查网络连接】的问题
- 优化功能:优化全局异常处理和日志文件

## v0.2.x (官方 / Official)

- v0.2.7:漏交 `helper.ts` 文件
- v0.2.6:功能优化
- v0.2.5:系统缺陷修复
- v0.2.4:系统缺陷修复
- v0.2.3:系统功能优化
- v0.2.2:修复系统获取不到分支的初始提交导致报错
- v0.2.1:修复系统初次同步导致数据丢失
- v0.2.0:新增功能开发

## v0.1.x (官方 / Official)

- v0.1.11 / v0.1.10 / v0.1.9 / v0.1.6 / v0.1.2:系统缺陷修复
- v0.1.8 / v0.1.7 / v0.1.3:系统功能优化
- v0.1.5 / v0.1.4:合并 dev 分支(Merge pull request)
- v0.1.1 / v0.1.0-beta:GIT 同步插件内测版